ifeq ($(BTYPE), 3)
LIBS += -shared-libstdc++
else ifeq ($(BTYPE), 4)
LIBS += -shared-libstdc++
else
LIBS += -lstdc++
endif

.IGNORE: distclean
distclean: clean
	$(call remove_files, $(DEPS))
	$(call remove_file, $(BIN_DIR)/$(TARGET)$(SUFFIX))

