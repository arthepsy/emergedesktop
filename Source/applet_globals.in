ifeq ($(MINGW64), 0)
		LIBS += -lstdc++_s
else
		LIBS += -lstdc++.dll
endif

.IGNORE: distclean
distclean: clean
	$(call remove_files, $(DEPS))
	$(call remove_file, $(BIN_DIR)/$(TARGET)$(SUFFIX))

